import Vue from "vue";

import vInputPassword from "@/components/base/v-input-password/v-input-password.vue";
Vue.component("v-input-password", vInputPassword);

export default {
  components: {
    vInputPassword
  }
};
